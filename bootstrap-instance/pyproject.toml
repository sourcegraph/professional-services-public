[project]
name = "sourcegraph-bootstrap"
version = "0.1.0"
description = "Initial setup of a new Sourcegraph instance"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.4",
]
